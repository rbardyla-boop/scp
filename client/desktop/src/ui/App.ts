// Phase 3: SCP desktop UI — tunnel corridor list + handshake flow.
// TODO: implement corridor visualization using Tauri invoke() calls into Rust core.

export function renderCorridorList(_corridors: unknown[]): void {
    // TODO Phase 3: render trusted corridor list
}

export function renderHandshake(): void {
    // TODO Phase 3: local QR / BLE handshake flow
}
